{
  "name": "okru-proxy",
  "version": "1.0.0",
  "description": "ok.ru video proxy — resolves IP-locked CDN URLs server-side",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "express": "^5.2.1",
    "node-fetch": "^3.3.2"
  }
}
